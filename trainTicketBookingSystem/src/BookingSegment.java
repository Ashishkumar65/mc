public class BookingSegment {
    public Station fromStation;
    public Station toStation;

    public BookingSegment(Station fromStation, Station toStation) {
        this.fromStation = fromStation;
        this.toStation = toStation;
    }
}
